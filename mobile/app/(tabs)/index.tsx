import { View, Text, Platform } from 'react-native';
import "@/global.css"

export default function HomeScreen() {
  return (
      <View className="flex-1 justify-center items-center">
        <Text className="text-5xl text-dark-200 font-bold">Pakyu HAn</Text>
      </View>
  );
}

